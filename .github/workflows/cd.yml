name: 🐳🚀 Build and Deploy

on:
  push:
    branches: [main, dev]
  workflow_dispatch:
    inputs:
      build_and_push_image:
        description: '🛠️ Build and push image'
        required: false
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'true'
      deploy:
        description: '🚀 Deploy (if false, only build)'
        required: false
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'true'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  APP_IMAGE_NAME: 'ghcr.io/${{ github.repository_owner }}/susu-time-machine'
  LOCAL_IMAGE_NAME: 'susu-time-machine:local'
  REMOTE_APP_DIR: '/app/susu-time-machine'
  IMAGE_ARCHIVE: 'susu-time-machine-image.tar.gz'

jobs:
  build-docker-image:
    name: 🐳 Build Docker Image
    if: ${{ github.event_name == 'push' || github.event.inputs.build_and_push_image == 'true' || github.event.inputs.deploy == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: 🔑 Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: 🐳 Build APP image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          load: true
          tags: |
            ${{ env.APP_IMAGE_NAME }}:latest
            ${{ env.APP_IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🚢 Push APP image to GHCR
        if: ${{ github.event_name == 'push' || github.event.inputs.build_and_push_image == 'true' }}
        run: |
          docker push "${{ env.APP_IMAGE_NAME }}:latest"
          docker push "${{ env.APP_IMAGE_NAME }}:${{ github.sha }}"

      - name: 📦 Save APP image for direct server upload
        run: |
          docker tag "${{ env.APP_IMAGE_NAME }}:latest" "${{ env.LOCAL_IMAGE_NAME }}"
          docker save "${{ env.LOCAL_IMAGE_NAME }}" "${{ env.APP_IMAGE_NAME }}:latest" "${{ env.APP_IMAGE_NAME }}:${{ github.sha }}" | gzip > "${{ env.IMAGE_ARCHIVE }}"
          ls -lh "${{ env.IMAGE_ARCHIVE }}"

      - name: ⬆️ Upload image archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-image
          path: ${{ env.IMAGE_ARCHIVE }}
          retention-days: 1

  deploy-to-server:
    name: 🚀 Deploy to Server
    needs: build-docker-image
    if: ${{ (github.event_name == 'push' || github.event.inputs.deploy == 'true') && (needs.build-docker-image.result == 'success' || needs.build-docker-image.result == 'skipped') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v5

      - name: ⬇️ Download image archive artifact
        uses: actions/download-artifact@v5
        with:
          name: app-image
          path: .

      - name: 🗂️ Prepare remote directory
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p "${{ env.REMOTE_APP_DIR }}"

      - name: 📤 Upload deploy files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: docker-compose.yml,${{ env.IMAGE_ARCHIVE }}
          target: ${{ env.REMOTE_APP_DIR }}
          overwrite: true

      - name: 🌐 Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail

            mkdir -p "${{ env.REMOTE_APP_DIR }}/data"
            mkdir -p "${{ env.REMOTE_APP_DIR }}/backup"

            cat > "${{ env.REMOTE_APP_DIR }}/.env" <<'EOF'
            ${{ secrets.APP_ENV_FILE }}
            EOF

            grep -q '^AUTH_SECRET=' "${{ env.REMOTE_APP_DIR }}/.env" || {
              echo "ERROR: APP_ENV_FILE must include AUTH_SECRET"
              exit 1
            }

            cd "${{ env.REMOTE_APP_DIR }}"

            if [ -f "${{ env.REMOTE_APP_DIR }}/data/app.db" ]; then
              cp "${{ env.REMOTE_APP_DIR }}/data/app.db" "${{ env.REMOTE_APP_DIR }}/backup/app-$(date +%Y%m%d-%H%M%S).db"
              find "${{ env.REMOTE_APP_DIR }}/backup" -name "app-*.db" -mtime +14 -delete || true
            fi

            export APP_IMAGE="${{ env.LOCAL_IMAGE_NAME }}"
            export DOCKER_DATABASE_URL="file:/app/data/app.db"
            docker load -i "${{ env.IMAGE_ARCHIVE }}"
            docker compose up -d --remove-orphans app
            rm -f "${{ env.IMAGE_ARCHIVE }}"
